--stub to track folder

This is where the logs for each participant are stored.
They are anonymized using a randomly generated 7 digit string.

Further, the amount of previous games as well as everything that speech detection finds is reccorded.